import ReactDOM from 'react-dom';
const root = document.getElementById('root');

function App() {
    return (
        <div>
            Hello World!
        </div>
    );
}

ReactDOM.render(<App/>, root);
